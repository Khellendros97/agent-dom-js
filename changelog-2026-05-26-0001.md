# Changelog-2026-05-26-0001

## 变更概述

修复 `snapshot()` 生成页面快照时遗漏 `<p>` 标签文本内容的问题。

## 问题描述

`BASE_SELECTORS` 中包含了 `h1-h3` heading 标签作为结构性内容输出，但页面正文的主要载体 `<p>` 标签未被包含，导致快照无法反映页面正文文本内容。

## 变更内容

### 1. `src/snapshot.ts`

- **BASE_SELECTORS** 中新增 `'p'` 选择器
- 新增 `p` 标签处理逻辑：与 heading 类似，作为纯文本行输出（不分配 ref），格式为 `- text "内容"`

### 2. `test/snapshot.test.ts`

- 新增测试 `captures paragraph text`：验证 `<p>` 标签文本被正确捕获，且不分配 ref
- 新增测试 `skips empty paragraphs`：验证空 `<p>` 标签不产生输出

## 关联文件

- `src/snapshot.ts` — 快照生成核心逻辑
- `test/snapshot.test.ts` — 快照测试

## 运行结果

```
 Test Files  9 passed (9)
      Tests  32 passed (32)
```

---

## Review-0001

### 审查结论

本次变更能正确捕获普通 `<p>` 文本，并复用了 heading 的纯文本输出模式；基础用例覆盖了正文段落和空段落。但当前实现存在一个回归风险：所有 `<p>` 会在 `snapshot.ts:62` 提前返回，导致原本可通过 `[role]`、`[tabindex]`、`[contenteditable="true"]` 捕获为可交互节点的 `<p>` 不再分配 ref。

### 整改 Todo

- [x] 修复 `src/snapshot.ts:62`：仅将普通 `<p>` 输出为 `- text`；带 `role`、`tabindex` 或 `contenteditable="true"` 的 `<p>` 应继续走交互节点逻辑并分配 ref。
- [x] 在 `test/snapshot.test.ts` 增加回归测试：`<p role="button">`、`<p tabindex="0">`、`<p contenteditable="true">` 应出现在 `snapshot.nodes` 中。
- [x] 强化 `test/snapshot.test.ts` 空段落测试：断言 `snapshot.text` 为 `''` 且 `snapshot.nodes` 长度为 `0`。
- [x] 补充测试：验证 `denySelectors` 对 `<p>` 生效。

### 整改报告

1. `src/snapshot.ts:62` — 将 `tag === 'p'` 改为 `isPlainParagraph` 判断，检查 `role`、`tabindex`、`contenteditable` 属性后再决定走 text 还是交互节点逻辑
2. `test/snapshot.test.ts` — 新增 `treats interactive p tags as interactive nodes, not plain text` 回归测试（覆盖 `role="button"`、`tabindex="0"`、`contenteditable="true"` 三种场景）
3. `test/snapshot.test.ts` — 强化 `skips empty paragraphs` 测试，增加 `toBe('')` 和 `toHaveLength(0)` 断言
4. `test/snapshot.test.ts` — 新增 `respects denySelectors for paragraph text` 测试

### 测试结果（整改后）

```
 Test Files  9 passed (9)
      Tests  34 passed (34)
```

---

## Review-0002

### 审查结论

上一轮 Review-0001 的主要问题已基本修复：普通 `<p>` 会输出为 `- text`，`role`、`tabindex`、`contenteditable="true"` 的 `<p>` 会进入交互节点逻辑；空段落断言和 `denySelectors` 测试也已补齐。

但仍有一个遗漏风险：`contenteditable` 属性的有效值不仅仅是 `"true"`。

### 整改 Todo

- [x] 将 `isPlainParagraph` 的 contenteditable 判断改为识别 `""`、`"true"`、`"plaintext-only"`，并排除 `"false"`
- [x] 补充 `<p contenteditable>`、`<p contenteditable="plaintext-only">` 回归测试
- [x] 补充 `<p contenteditable="false">` 测试，确认其仍按普通段落处理

### 整改报告

1. `src/snapshot.ts:62` — 将 `element.getAttribute('contenteditable') !== 'true'` 改为 `ce !== null && ce !== 'false'`，覆盖了 `""`（空属性）、`"true"`、`"plaintext-only"` 三种可编辑场景
2. `test/snapshot.test.ts` — 在 `treats interactive p tags` 测试中新增 `contenteditable`（空值）和 `contenteditable="plaintext-only"` 两个用例，nodes 断言从 3 更新到 5
3. `test/snapshot.test.ts` — 新增 `treats contenteditable=false as plain paragraph` 测试，确认 `contenteditable="false"` 仍走文本输出

### 测试结果（整改后）

```
 Test Files  9 passed (9)
      Tests  35 passed (35)
```
